<div class="container">
  <h1>Tasks</h1>

    Stay consistent with your habits
    Every time you do the thing, there's a 20% chance that
    you can reward yourself for being awesome.
    <br/>
    This app uese reward prediction error.
    <br/> <br/>
    Learn more about reward prediction error in the time stamp on <%= link_to "this video", "https://youtu.be/vA50EK70whE?t=3003" %> (00:50:03)

    <br/><br/><br/>
  <%= link_to "New Task", new_task_path, class: 'btn btn-primary' %>
  <br/>
  <div class="card">

    <table>
      <tr>
        <th>Log</th>
        <th>Task Name</th>
        <th>Duration</th>
        <th>Time</th>
        <th>Frequency</th>
        <th>Logged</th>
        <th>Rewards</th>
      </tr>
      <% @tasks.each do |task| %>
          <tr>
              <td><%= link_to "+", new_task_log_path(task), class: 'btn btn-success' %></td>
              <td><%= link_to task.name.titleize, task_path(task) %></td>
              <td><%= task.required_time_in_minutes %></td>
              <td><%= task.time_of_day %></td>
              <td><%= task.frequency %></td>
              <td><%= task.logs.count %></td>
              <td><%= task.logs.select {|l| l.gave_reward? }.count %></td>
          </tr>
      <% end %>
    </table>
  </div>
</div>
